# Heading 1 一级标题

段落文本用于测试 `p` 标签样式。这一行包含 **粗体 strong**、*斜体 em*、~~删除线 del~~，以及 [超链接 a](https://example.com)。

## Heading 2 二级标题

段落文本。内联代码 `const x = 42;` 穿插在句子中。这是另一段文本用于测试默认的 **行高 line-height** 和字色。

### Heading 3 三级标题

段落文本。再次强调 **加粗文字** 在段落中的呈现效果，以及 *倾斜文字*、~~被删除的文字~~ 和 [另一个链接](https://obsidian.md)。

#### Heading 4 四级标题

一段普通的段落文本，用于测试不同层级标题下的段落间距和排版效果。

##### Heading 5 五级标题

段落文本。测试连续段落之间的间距是否合适。

###### Heading 6 六级标题

段落文本。六级标题应该以 muted 颜色呈现。

---

## 无序列表

- 无序列表项一
- 无序列表项二
  - 嵌套无序列表项
  - 另一个嵌套项
- 无序列表项三

## 有序列表

1. 有序列表项一
2. 有序列表项二
   1. 嵌套有序列表项
   2. 另一个嵌套项
3. 有序列表项三

---

## 引用块

> 这是一段引用文本。引用块应有左侧彩色边框、背景色和 muted 字色。
>
> 引用块内的 *斜体* 和 **粗体** 也应正常工作。
>
> > 嵌套引用块

---

## 代码

行内代码：使用 `console.log("hello world")` 输出信息。

代码块：

```javascript
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log(fibonacci(10)); // 55
```

---

## 表格

| 名称 | 类型 | 描述 |
|------|------|------|
| id | number | 唯一标识 |
| name | string | 名称 |
| active | boolean | 是否激活 |
| createdAt | date | 创建时间 |

---

## 分隔线

上面的分隔线 `hr` 样式应该是一条细线，颜色为 `--background-modifier-border`。

---

![图片占位](https://picsum.photos/400/200)

---

::select
Q1: 以下哪一事件标志着二战太平洋战场的结束？

[A] 珍珠港事件
[B] 日本投降
[C#] 广岛原子弹
[D] 德国投降
::

> 提示：测验组件和引用块可以紧邻出现，验证样式隔离

==高亮显示==